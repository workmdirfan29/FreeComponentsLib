import React from "react";

const GhostButton: React.FC = () => {
  return (
    <button className="px-3.5 outline-none text-sm font-semibold rounded-md hover:bg-zinc-900 border-none py-2.5 text-zinc-50">
      Ghost
    </button>
  );
};

export default GhostButton;
